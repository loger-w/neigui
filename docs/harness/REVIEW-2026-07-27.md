# Harness 改版 review 交接(2026-07-27 批次)

> 給新 session 的 review 入口。真實檔在 `~/.claude/`,repo 內 `docs/harness/` 是鏡像
> (`python scripts/sync-harness-mirror.py --check` 驗漂移,review 當下應「全部一致」)。
> 改動依據在 `~/.claude/harness/RATIONALE.md` 的 2026-07-27 條目(/feat 節首條 + 頁頭
> 記錄紀律行),**review 前先讀它**,不要憑本檔摘要重推。
>
> 本批**不需要 fresh CLI process**:改動全是流程時載入的檔(feat.md / auto-verify /
> refs / RATIONALE),無常駐層 / SessionStart 變更,/clear 開新對話即可。

## 本批 commits(neigui main)

| commit | 內容 |
|---|---|
| `e0a9bef` | 07-26 批次二輪復審記錄 + RATIONALE 拆帳 6→7 更正(以 upstream diff 實數) |
| `2ec17c6` | next-time 補「mirror 是否納 6 支複製 skill」評估項(P3) |
| `8c0c208` | **本批主體**:e2e 意圖對齊 P1(三槓桿)+ Phase 4 (b) 裁決句 + 記錄紀律 |

不入 git 的變更(直接看 `~/.claude/`):`feat-improvements.md` 07-27 三條標 resolved
(user 直接拍板,沿 07-26 前例不走排程 meta-review;收件匣未結案回到 5 條)。

## 改動摘要(review 對象 = 8c0c208 的五個落點)

根因一條(RATIONALE 有完整敘述):e2e assertion 是模型從討論轉譯的,轉譯錯照樣綠 —
user 原話「常常發現 AI 雖然會用測試環境 E2E 但結果根本不是使用者想要的畫面或功能」。

1. **feat.md Phase 0 SC gate**:UI / 畫面類 SC 必寫「畫面可指認」表述(位置 / 文字 /
   顏色 / 元素),e2e assertion 與截圖驗收都以此為準。
2. **feat.md Phase 6 步驟 2 + auto-verify v3.1.0 Subsumed 節**(兩處同步):subsumed
   限縮到**純 regression SC**;本輪新增 / 改動的 UI SC 第一輪一律真截圖。
3. **refs/feat-phase8.md 步驟 4**:UI 驗收點 — 收尾回報必逐條附 UI SC 截圖路徑 +
   畫面內容描述;「回報裡沒出現 = 未完成收尾」。
4. **feat.md Phase 4 步驟 1「Diff 先落檔」例外句**:(b) missing-from-spec 焦點必讀
   design.md 的 SC / 介面節(dispatch prompt 附行號範圍),不受「按需才開檔」裁量。
5. **RATIONALE.md**:頁頭「記錄紀律」(計數敘述必附產生指令以輸出回填)+ /feat 節
   2026-07-27 來歷條目。

## Review 角度建議(對抗式,不是背書)

- **subsumed 限縮的下游一致性**:feat.md Phase 7 步驟 3 的 real-env 例外欄仍寫
  `subsumed by Phase 5: <e2e spec#>` 未動 — 檢查「Phase 6 端把關、Phase 7 只收標記」
  的分工是否成立,還是 Phase 7 例外欄需要同步加「僅 regression SC」限定語。
- **兩處 subsumed 文字的語意漂移**:feat.md Phase 6 版與 auto-verify 版是各自改寫的
  句子(非逐字複製)— 逐字對讀,確認限縮範圍(「UI SC」vs「UI」)與判準無縫隙。
  另注意專案 CLAUDE.md §1 有第三處相關文字(「UI 改動還要走 chrome-devtools-mcp
  真實截圖驗證」)— 三處是否互相矛盾。
- **Phase 4 (b) 例外句的反轉幅度**:原成本優化動機(subagent 重複讀 design.md 全檔
  佔 /feat 成本大宗)是否被過度反轉 — 例外句限定「SC / 介面節 + 行號範圍」,檢查
  表述會不會被執行者讀成「design.md 全檔必讀」而吃回成本。
- **UI 驗收點的機械可驗性**:「回報裡沒出現 = 未完成收尾」沒有 hook / script 承接,
  是純紀律條文 — 對照 RATIONALE 07-26 通則「新條文要嘛產 artifact,要嘛別寫」,
  這條的 artifact 是收尾回報本身,是否站得住?與 branch-lifecycle 收尾節順序有無衝突。
- **/mod 側繼承問題**:mod.md Phase 5 cross-ref feat.md Phase 4 dispatch 紀律 —
  (b) 焦點是 /feat 雙焦點專屬還是 /mod 也有等價物?新例外句經 cross-ref 是否自動
  生效,或成為 /mod 側既有 2 P1 + 15 P2 之外的新缺口。
- **UI SC gate 與上游 skill 的相容**:brainstorming skill 的對話流程 + e2e-conventions
  判準表,與「畫面可指認」表述要求有無流程順序衝突(SC 定稿時 e2e 歸屬未定)。
- **記錄紀律的自指檢查**:RATIONALE 新來歷條目自己有沒有守「計數附產生指令」;
  本交接檔的計數(五個落點 / 三條 resolved / 5 條未結案)一併驗。
- **07-26 二輪復審(e0a9bef)的記錄面**:REVIEW-2026-07-26.md 新增節的宣稱
  (原生自證三項 / 機械四項)與 RATIONALE 拆帳更正,抽驗 1-2 項可重跑者。

## 機械驗證

- `python scripts/sync-harness-mirror.py --check` → 全部一致。
- `cd ~/.claude/hooks && python -m pytest tests -q` → 基準 130 passed。
- 五個落點各 grep「2026-07-27」標記句存在(review 補修後實測 `grep -c "2026-07-27"`:
  feat.md ×4、auto-verify SKILL.md ×1、feat-phase8.md ×1;**原宣稱 feat.md ×3 /
  auto-verify ×1 為憑記憶回填,實測 auto-verify 為 ×0** — 見下方 review 結果 P1);
  auto-verify frontmatter version = 3.1.0。
- `~/.claude/feat-improvements.md` 未結案節:07-27 三條均為 [resolved],未結案剩
  07-25 ×2 + 07-26 ×3 = 5 條。

## 已知未修(不要當新發現回報)

- /mod 側 2 P1 + 15 P2(07-26 一致性掃描,next-time.md 末節列管)。
- 收件匣 5 條未結案(07-25 harness-context-slimdown ×2 + 07-26 cost-research ×3),
  等 meta-review 或 /mod 側改版一起收。
- 6 支複製 skill 無 VCS(next-time.md 已列 mirror 擴充評估項)、dispositions.json
  過期 rows、graphify 640 dangling edges。
- writing-plans L158 殘句「using executing-plans」(REVIEW-2026-07-26 二輪復審節
  P3 觀察,fallback 自含不擋路)。

## Review 結果(2026-07-27,/clear 新對話對抗式復審;判定:不退回,P1+P2 已修)

- **機械驗證**:mirror --check 全一致、hooks pytest 130 passed、feat-improvements 計數
  相符、07-26 二輪復審機械宣稱獨立重跑 3/4 過(mirror / pytest / superpowers grep 零殘留,
  含嵌套 writing-good-tests.md)— 唯標記句一項不實,即 P1。
- **P1 ×1(已修)**:auto-verify Subsumed 限縮句無「2026-07-27 拍板」標記
  (`grep -c` = 0,本檔原宣稱 ×1)— marker 還原路徑對該處失效 + 記錄紀律拍板同批即違反。
  修:限縮句補標記,本檔機械驗證節以實測回填。
- **P2 ×3(已修 2、列管 1)**:
  1. feat.md Phase 7 例外欄 `subsumed by Phase 5` 無 regression-only 限定(Phase 6 錯誤
     subsumed 在 Phase 7 機械欄檢查會放行)→ 補「僅純 regression SC 可標」限定語。
  2. Phase 4 (b) 例外句「SC / 介面節」不在 Phase 1 模板章節詞彙(架構/檔案組織/資料流/
     邊界/接點),行號圈定留解釋空隙 → 改「SC 對應章節與接點節」+ 註明以實際結構為準。
     (「會被讀成 design.md 全檔必讀」的反向疑慮判不成立 —「附行號範圍」邊界明確。)
  3. /mod 側三槓桿缺口:可指認 SC 表述與 UI 驗收點缺席、subsumed 限縮經 auto-verify 已
     自動生效 — 不在 07-26 已知 2P1+15P2 清單(清單早於本批槓桿)→ 記 next-time.md
     /mod 改版節,不本批修。
- **觀察 ×2(不動)**:UI 驗收點對照「新條文要嘛產 artifact」通則判站得住(收尾回報
  user 每輪可見,user 本人即 gate;PR body 段 4 已有截圖路徑軌),但無機械承接是事實,
  要強化可 state.json 加 `ui_acceptance` 欄;過目點落在自動 merge 之後屬偵測型非閘門型,
  與鐵則 H 一致。
- **PASS**:三處 subsumed 文字無矛盾(「UI SC」vs「UI」是刻意框架差,auto-verify 服務五
  流程無 SC 語彙;CLAUDE.md §1 管「改動」與 subsumed 管「regression」不相交);
  brainstorming / e2e-conventions 無順序衝突(可指認表述與「資料級 assertion」教訓互補,
  正是 07-07 visibility-only 事故的上游補強);RATIONALE 新條目自身計數枚舉自洽。

## 還原路徑(review 判定要退回時)

- 全部條文都帶「2026-07-27 拍板」標記句:git revert `8c0c208`(鏡像)+ 真實檔
  `~/.claude/` 各處刪標記句(mirror 是單向 real→mirror,revert 鏡像不會自動改真實檔)。
- auto-verify version 3.1.0 → 3.0.0 隨句回退。
- feat-improvements.md 三條 [resolved] 改回 [proposed]。
