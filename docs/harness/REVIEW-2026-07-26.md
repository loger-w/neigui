# Harness 改版 review 交接(2026-07-26 批次)

> 給新 session 的 review 入口。真實檔在 `~/.claude/`,repo 內 `docs/harness/` 是鏡像
> (`python scripts/sync-harness-mirror.py --check` 驗漂移,review 當下應「全部一致」)。
> 改動依據全部落在 `~/.claude/harness/RATIONALE.md` 的三個 2026-07-26 節,**review 前先讀它**,
> 不要憑本檔摘要重推。

## 本批 commits(neigui main)

| commit | 內容 |
|---|---|
| `5d6c2cd` | /feat 實證改版本體:review 降 1 輪制、廢 never-fired 機制、graphify 接入 |
| `77d502f` | next-time 補後續清單 |
| `24308ec` | superpowers plugin 停用(第一段:未複製) |
| `4d679d6` | 改版後一致性修正(1 P0 + feat 側 7 P1:PHASE_GATES / 限縮輪 / reviewer effort) |
| `9d90721` | twse-tpex skill description 去重複句 |
| `0cbc0aa` | superpowers 6 支複製為 user skill + harness 引用全面去前綴(第二段) |

不入 git 的變更(直接看 `~/.claude/`):`settings.json`(plugin off + skillOverrides 5 支)、
neigui `.claude/settings.local.json`(supabase 移除 + 2 支 off)、`~/.claude/skills/` 新增 6 個
複製目錄、`graphify-out/`(gitignored,code-only 圖 3227 nodes)。

## 改動摘要(review 對象)

1. **Review 輪數**:spec review 1 輪制(design 有 accepted P0 → 限縮加輪至多 1 次,只審
   amendment;impl-spec 固定 1 輪);code review 單輪深度優先。依據:41 run artifact 分析
   (round 2+ 有 38% 零 accepted P0/P1;9 常規 run 的 round 2/3 只有 1 條 reviewer 獨立新 P0)。
2. **Finding 處置分級**:spec finding 免逐條 receiving(99.6% accept 率 = 儀式),code review
   維持全紀律(24.3% 誤報率,把關真實)。
3. **廢除**:per_file 模式(0/7)、「Findings>10 收斂」(0/11)、Phase 4 inline 自查(3/16 且
   07-07 後全 0)、`sc_cycle_counts`(→ `rollbacks` append log)。
4. **新增**:graphify query 優先(Phase 0-2,CLI 直呼)+ Phase 8 `--update`;Phase 4 diff 先落檔;
   Phase 6 real-env finding commit 文法;Phase 7 固定檔名 `phase7-verification.md`;
   「同 phase 多檔單一 message 平行 Read」。
5. **superpowers**:plugin 停用;brainstorming / writing-plans / test-driven-development /
   receiving-code-review / verification-before-completion / systematic-debugging 六支複製為
   user skill,全部引用去 `superpowers:` 前綴;殘留前綴引用皆條件式罕用項(worktrees /
   finishing / executing-plans / SDD 負向)。
6. **reviewer agents**:effort 統一 high(鐵則 G round 3 量測);design-reviewer 補「限縮輪」節;
   impl-spec-reviewer 刪 per_file / round≥2。

## Review 角度建議(對抗式,不是背書)

- **砍過頭檢查**:每條被刪機制對照 RATIONALE 實證數字——樣本是否支撐刪除?特別檢查
  「design review 限縮輪」是否足以接住 e2e-tests 型的大翻修 case(當時靠 tech pivot 條款,
  確認該出口仍在 feat.md Phase 1 步驟 4)。
- **skill 複製的完整性**:6 支複製目錄 vs plugin cache 原始目錄 diff(來源
  `~/.claude/plugins/cache/temp_git_*/skills/`;注意複製來源是 marketplace clone 而非
  claude-plugins-official 正式 cache,確認兩者版本無差異)。
- **去前綴 sweep 的漏網**:`grep -rn "superpowers:" ~/.claude/commands ~/.claude/harness
  ~/.claude/skills/branch-lifecycle ~/.claude/skills/auto-verify ~/.claude/CLAUDE.md`
  —— 應只剩 using-git-worktrees ×2 / finishing ×2 / executing-plans ×1 / SDD ×1(負向)。
- **新 session 才能驗的**(本批唯一無法在改動 session 自證的):skill 清單應含無前綴 6 支、
  無任何 `superpowers:*`、無 mlb / worldcup / tw-market-research / neoapi / vercel;
  SessionStart 不再注入 using-superpowers 全文。
- **機械驗證**:`cd ~/.claude/hooks && python -m pytest tests -q`(基準 130 passed)、
  `python scripts/sync-harness-mirror.py --check`、
  `cd C:/side-project/neigui && graphify query "任一 caller 問題"`(圖可用性)。
- **已知未修**(不要當新發現回報):/mod 側 2 條 P1 + 15 條 P2(一致性掃描,見 next-time.md
  末節)、dispositions.json 過期 rows(RATIONALE 已列)、graphify 圖 640 dangling edges
  (AST 對外部 import 的正常樣態)。

## Review 結果(2026-07-27 已執行,對抗式)— 新 session 不需重跑本批

**總判定:改版本體全部落地且自洽,零退回。** 全部 findings 已當場處置完畢
(修復 commit:`ed58255` P1 / `7243b22` P2)。

### 機械驗證(全過)

- hooks pytest **130 passed**(=基準);`sync-harness-mirror.py --check` 全部一致;
  graphify query 可用(caller 問題實測回 61 nodes)。
- 去前綴 sweep:殘留恰為預期清單(worktrees ×2 / finishing ×2 / executing-plans ×1 /
  SDD ×1 負向),其餘命中皆 RATIONALE 史料 / CLAUDE.md 告示 / dispositions 快照。
- 6 支複製 skill 與 marketplace clone **及** official 6.2.0 cache 內容全一致
  (交接檔擔心的兩來源版本差**不存在**;各目錄多一個 `.orphaned_at` 標記檔,無害,
  機制備註已入 RATIONALE)。
- 砍過頭檢查逐條對過 RATIONALE 數字,每條刪除都有承接;**tech pivot 出口確認仍在**
  feat.md Phase 1 步驟 4(user 三選一,批准後輪數歸零)。PHASE_GATES 已同步 1 輪制、
  check_feat_tags 豁免 (b) 與 feat.md Phase 6.4 文法一致、廢除欄位 hooks/refs 零功能殘留。

### 新 session 驗證三項:PASS(注意誤診陷阱)

headless 新 process 探針:無任何 `superpowers:*`、無前綴 6 支在列、SessionStart 無注入;
陽性對照(已啟用的 chrome-devtools-mcp skill 有出現)排除「headless 不載 plugin」假陰性。
skillOverrides 與專案 local off(mlb / worldcup / tw-market-research / neoapi / vercel)全生效。

**誤診紀錄(防重蹈)**:07-26 晚間的舊 process(startup #476)啟動早於停用寫入(23:37),
該 process 的 session(含 /clear 延續)仍會看到 superpowers 完整載入 —— 這是 process
時序,**不是停用失效**。曾據此誤判 P0,已用 `numStartups` / `lastUsedNumStartups` 對時
戳澄清 + CLI `claude plugin list` 確認兩 scope 均 disabled。互動 session 要看到乾淨
狀態需重啟 CLI process。

### Findings 與處置

| 級別 | 內容 | 處置 |
|---|---|---|
| P0(撤銷) | 「plugin 停用無效」 | 誤診(上述 process 時序),設定面無事可修 |
| P1(已修) | 6 支複製件**內文**殘留 `superpowers:` 交叉引用 7 處 3 檔(systematic-debugging 死指標 ×2、writing-plans REQUIRED SUB-SKILL 與 feat.md:76 衝突 ×3、文案 ×2) | `ed58255`:去前綴 / 改「執行方式由呼叫方流程決定」;**上游重複製會帶回死指標,重複製後必重跑 sweep**(RATIONALE 有記) |
| P2 ×3(已修) | RATIONALE (d) 條「作廢」與 review-protocol B 節活規則矛盾;`.orphaned_at` 機制未記;inline 自查「機械覆蓋」實為四項之三(文件同步無承接) | `7243b22`:三處 RATIONALE 記錄修正 |

### 遺留(本批不處理,原清單不變)

/mod 側 2 P1 + 15 P2、dispositions.json 過期 rows、graphify 640 dangling edges —
均已在 next-time.md 末節 / RATIONALE 列管,下輪 /mod 同步改版時收。

## 還原路徑(review 判定要退回時)

- 輪數 / 機制:git revert 對應 commit + `sync-harness-mirror.py --fix` 反向(鏡像→真實檔要手動)。
- superpowers:`settings.json` enabledPlugins 改回 true + 刪 `~/.claude/skills/` 的 6 個複製目錄
  (避免同名衝突)+ git revert `0cbc0aa` 的引用改名。
- skillOverrides / MCP:兩個 settings 檔各刪對應行。
