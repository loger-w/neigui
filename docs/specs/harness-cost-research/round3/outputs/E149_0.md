```json
{"findings":[
{"severity":"P1","location":"design.md:§5.1 / §5.6 / §5.8(條件式 ref 列)vs §3 P1","claim":"四支條件式 ref 中只有 feat-state.md 被明寫「指標句留核心」,其餘三支(feat-phase4-fix.md / auto-wave.md / branch-lifecycle references/exceptions.md)在 §5 表沒有任何「核心觸發句」列,而機械注入依設計刻意跳過 condition 項 —— 它們將永遠不會被載入。","why":"§3 P1 的整條原則是「觸發偵測留核心,處置細節可延後」,但處置表只把整段標 ref、沒有留下呼應的核心指標列。實裝的 harness_lib.py:70-71 明確 `if rec.get(\"condition\"): continue`(理由:每回合提醒是噪音,觸發條件寫在 command 核心)—— 也就是 hook 側正確地把責任推回核心檔,而 §5 表沒有履行這個約定。SC-3 也抓不到:條件式 ref 那幾列的 `present` 檢查指向落點檔,落點檔確實存在,全綠;沒有任何檢查會問「來源檔裡還有沒有那句觸發指標」。結果是 `[lock]`/mutation-verified 紀律(§6.1 明言 gate 照常有效)、goal_efficiency_mode wave 判定、branch-lifecycle 異常處理三塊在真實流程中靜默失聯。§8 風險表的「ref 該載入時沒載入」只列了「非 /feat 流程」與「Phase -1」兩個缺口,沒有涵蓋條件式這一類。","fix":"§5.1 / §5.6 / §5.8 各補一列:對應條件式 ref 的**觸發判定句留核心**(如「Phase 4 若收到 test-gap 類 finding → 讀 refs/feat-phase4-fix.md」),處置標 `核心`,並依 §5 規則 3 在 dispositions.json 填 `present`(file = 來源 command/skill 檔)。同時在 §4 檔案樹的條件式項旁註明「觸發句在核心,hook 不涵蓋 condition 項」。"},

{"severity":"P1","location":"design.md:§3 P1 + §4.2 + §7 步驟 5","claim":"PHASE_REFS 從編譯期常數改為 runtime 讀 load-manifest.json,把 §3 P1 花整段篇幅要避免的「靜默停注入」失效模式原封搬進了新機制,而且沒有任何 SC 驗證注入第四行真的出現。","why":"§3 P1 的論證是:加第三欄 → ValueError → 被 harness-context.py 兩層 except 吞掉 → 整段注入靜默消失。v3 起十輪都在守這一條。但同一節接著決定「PHASE_REFS 由 manifest 的 phase 欄產生,不手抄」,而實裝的 harness_lib.py:55-59 對 OSError / JSONDecodeError / KeyError / TypeError 一律 `return {}`(註解自陳 fail-open)。失效條件從「改 code 才會踩到、且會被既有測試擋」變成「一個 JSON 檔改壞、路徑挪動、profile key 改名就靜默失效」—— 而 manifest 本來就是本次改版要頻繁編輯的檔,還被步驟 7 拉進鏡像同步(多一條被自動化改動的路徑)。SC 表八條沒有一條驗「注入實際發生」:SC-3 只驗 dispositions、SC-4 只驗 hook 測試 passed 數(單元測試傳 claude_dir 參數,不會踩到真實路徑),SC-6 明文只跑 /mod 或 /bug(見另一條 finding),而該機制只涵蓋 /feat。整個 §3 P1 是本次改版「ref 不會該載入時沒載入」的唯一保障,卻是全 spec 唯一零驗收的機制。","fix":"兩件事任一即可堵住:(a) 加一條 SC — 在 /feat state.json 存在的情況下實跑一回合,附 hook 注入輸出截圖/文字,確認第四行含預期 ref 路徑;或 (b) 把 `_phase_refs` 的 fail-open 改成 fail-loud-but-degraded:manifest 存在卻解析失敗時在注入文字中印一行 `[harness] PHASE_REFS 讀取失敗: <reason>`(仍不 raise,不影響既有三行),讓靜默失效變成可見失效。建議兩者都做,並把「manifest 是 runtime 依賴」寫進 §8 風險表。"},

{"severity":"P1","location":"design.md:§1 SC-6 + §8 風險表","claim":"SC-6 明文只要求跑一個真實 /mod 或 /bug 小案子,但本次改版風險最集中的三塊(SDD 關閉後的三條紀律移植、PHASE_REFS 機械注入、feat.md 十餘列 ref 拆分)全部只存在於 /feat —— §8 風險表有兩條的緩解措施指名 SC-6,而 SC-6 的 scope 涵蓋不到它們。","why":"§8「關掉 SDD 後流程紀律流失 → 處置:SC-6 真實跑一輪驗紀律仍在」直接落空:SDD 三條紀律(ledger / 每 task review gate / 禁並行 implementer)寫在 refs/feat-phase3.md,只在 /feat Phase 3 載入,跑 /mod 或 /bug 一輩子碰不到。§3 P1 自陳機械注入「只涵蓋 /feat」,SC-6 卻排除 /feat,於是該機制在真實環境零覆蓋。此外若實作者選 /bug 而非 /mod,§5.5 bug.md 處置表中沒有任何 /code-review 列,「Workflow 驅動 review 未經真實流程驗證 → 處置:SC-6」這條緩解也一併落空 —— 也就是 SC-6 的二選一讓兩條風險緩解可被合法規避,而不是被繞過。這不是門檻鬆緊問題,是驗收對象與風險對象不相交。","fix":"SC-6 拆成兩條:SC-6a 跑一個真實 /mod 小案子(覆蓋 review-protocol 改寫 + 非 feat 流程的核心檔指標句);SC-6b 跑一個真實 L 級或 M 級 /feat 小案子,驗收項明列:hook 注入的 ref 行、Phase 3 三條紀律實際被遵守(ledger 檔存在、每 task 有 review gate、無並行 implementer dispatch)、條件式 ref 至少一支被正確觸發載入。若嫌成本高,SC-6b 可降為「跑到 Phase 4 為止」,但不得以 /mod 或 /bug 代替。"},

{"severity":"P1","location":"design.md:§1 反作弊規則 + §2.1 計帳邊界","claim":"反作弊規則只堵了「砍核心」與「縮 before 分母」兩條路,漏掉最誘人的第三條:把內容搬進 manifest 帳外的檔(常駐層 CLAUDE.md / MEMORY.md / RATIONALE.md),after 立刻變小而主 agent 實際窗口毫無改善,甚至變差。","why":"§2.1 把 main scope 定義為「command 檔 + 主 agent Read 的 refs + 主 agent Skill 呼叫的 skill」,常駐層明確不在內(§10 也把它劃為 Out of Scope)。於是把一段規則從 feat.md 搬進專案 CLAUDE.md,SC-1 與 SC-2 兩個降幅同時改善,而該規則從「只有跑 /feat 時載入」變成「每個 session 無條件載入」—— 窗口佔用實際上升,兩個 SC 卻同時報喜。這比另兩條作弊路徑更難察覺:它不觸犯「不得刪核心」(規則沒被刪、還被保留了),也不觸犯「-before 清單差異只能來自實際搬動的檔」(搬動確實發生了,只是落點在帳外)。SC-3 更是完全站在作弊者這邊:搬進 CLAUDE.md 的列可以合法填 `absent`(來源檔)+ `present`(CLAUDE.md),全綠。時機上這條路徑又特別現成 —— §10 已記載 user 正在自行改專案 CLAUDE.md §0(刪 21 行,未提交),實作者眼前就有一個「順手把東西挪過去」的活躍編輯面。","fix":"§1 反作弊規則加第 3 條:「不得將任何 §5 判為保留的規則移出 manifest 帳外的檔案。落點只能是 `harness/refs/`(計入 main scope)或 RATIONALE.md(且僅限純敘事/理由,不得含祈使句規則)。」並在 §5 表加一條硬性要求:任何 `移入` 列的 `present` 檢查,其 `file` 必須是 manifest 某個 profile 列出的路徑;`harness_load_estimate.py --verify-dispositions` 順便機驗這一點(它已經同時讀得到兩份 JSON,成本近乎零)。"},

{"severity":"P2","location":"design.md:§1 SC-4","claim":"SC-4 的不等式 `passed ≥ B − G 且 failed = 0` 偵測不到「既有 case 被刪或改名、由新增 case 補回數量」,而步驟 5、6 兩處紅先行保證了必然有新增 case 可以掩蓋損失。","why":"SC-4 的名字是「hook 測試不退步」,但它只比對一個純量。步驟 5 動 harness_lib.py、步驟 6 大改 check_feat_tags.py(--state 由 required 改 optional、加 --since、加 prefix 判定、加 emoji checker),兩處都在既有測試檔上動刀;只要新增的 case 數 ≥ 被移除的既有 case 數,SC-4 就綠。這正是鐵則 E 點名的錯型(砍測試 / 改 assertion 讓它過)在 gate 層的漏網版本 —— spec 用不等式解決了「等式恆 FAIL」的問題(v4 的正確修正),但沒補回等式原本提供的那部分保護。§6.2 第 6 步還特別註明「現有 TestMain 全部顯式帶 --state,把 required 改 optional 不會讓既有 case 變紅」,顯示作者確實在意既有 case 的存續 —— 但 SC-4 沒有把這個在意轉成可驗的東西。","fix":"SC-4 的量法改為記錄 nodeid 集合而非計數:步驟 0a 跑 `pytest tests --collect-only -q` 落檔成 `baseline-nodeids.txt`,步驟 11 要求 `(before_nodeids − push_gate_nodeids) ⊆ after_nodeids`(集合包含,允許新增、禁止淨損失),failed = 0 不變。差集非空即列出被移除的 nodeid 並停下說明。實作成本是兩條 shell 指令加一次 comm/diff。"},

{"severity":"P2","location":"design.md:§5「填檢查的兩條硬性要求」第 3 條 vs §1 SC-1","claim":"v10 新增的「核心列必填 present」規則要求對每條保留規則釘一段逐字錨,與 SC-1 的 25% 降幅門檻正面對抗,而 spec 沒有規定 present 錨該取多短或該取什麼粒度。","why":"§5 表中 `核心` 列佔大多數(feat.md 十餘列、perf/refactor/bug 幾乎全表)。每列都要一個能在來源檔 grep 命中的 present 字串,而步驟 2「逐檔改寫 command」的主要壓縮手法恰恰是壓句、刪 rationale、合併重述 —— 任何被壓到的核心規則,其 present 錨會 grep 不到,SC-3 判 FAIL,即使規則實際完好保留。實作者面對「SC-3 紅」和「SC-1 差 1,400 bytes」的雙重壓力,理性選擇是逐字保留所有核心句、放棄壓縮,然後 SC-1 未達標 → 觸發 §1 停下回報 → 三選一。也就是 v10 為堵「順手刪掉核心規則」加的保護,在 v11/v12 把門檻收緊到僅餘千餘 bytes 餘裕之後,變成 SC-1 的主要阻力,而這兩版都沒有回頭檢視這個交互作用。","fix":"§5 規則 3 補一句粒度定義:present 錨取**該規則的最短不可壓縮語意核心**(如動詞+受詞,`不准 N/A`、`mutation-verified`、`caller map 落檔`),明確禁止拿整句或含 rationale 的長片段當錨;並在步驟 1d 加一句「若壓縮後錨失效,改錨而非還原措辭 —— 但改錨必須在 commit message 記一行,供步驟 11 覆核」。這樣既保留「核心規則被順手刪掉會被抓到」的覆蓋,又不把壓縮空間鎖死。"}
]}
```