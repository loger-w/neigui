```json
{"findings":[
{"severity":"P1","location":"design.md:§1 SC-6 / §8 風險表","claim":"SC-6 限定用 /mod 或 /bug 實測,但本次改版風險最大的機制全部只在 /feat 路徑上,永遠不會被 SC-6 走到","why":"SDD 三條移植紀律在 refs/feat-phase3.md(只有 /feat Phase 3 載入)、PHASE_REFS hook 注入只涵蓋 /feat(§3 P1 已知限制)、feat-phase4-fix 條件式載入也是 /feat 專屬。而 §8 風險表把「關掉 SDD 後流程紀律流失」的處置寫成「SC-6 真實跑一輪驗紀律仍在」—— 用一個結構上碰不到該紀律的流程去驗該紀律,這條 mitigation 是空的;/mod /bug 只會過到 review-protocol 一小段","fix":"SC-6 改為必須含一個真實 /feat 小案子(或至少一個會進 Phase 3 implementer fan-out 的案子),明確要求驗證項清單:ledger 檔有建、每 task review gate 有跑、hook 第四行 ref 注入有出現;/mod /bug 案子可另跑但不能替代"},
{"severity":"P1","location":"design.md:§4.2 / §1 反作弊規則","claim":"SC-2 的分子(after profile)完全靠手寫 manifest 自我申報,沒有任何機驗把它對到改版後 command 檔實際指向的 refs","why":"反作弊規則只封了 before 側(不准縮分母)與「清單差異來源」,§8 也只驗「manifest 路徑必須存在」(exit 非 0)。但反向漏洞沒人管:改寫後 feat.md 新增了指向某 ref 的指標句、manifest 卻漏列該檔,載入量被低估、降幅被高估,SC-2 假綠 —— 這與「縮分母」是同一類量法失真,卻無 gate。整份 spec 對「刪」有 dispositions 驗、對「量」的 after 側只有信任","fix":"harness_load_estimate.py 加一個 --check-manifest 模式:grep 六個 command 檔與各 ref 檔中所有 `refs/*.md` / `references/*.md` 指涉,與 after profile 的 files 清單做集合比對,漏列或多列皆 exit 非 0;納入 SC-3 或步驟 11"},
{"severity":"P1","location":"design.md:§7 步驟 1 vs 步驟 0a 註","claim":"步驟 1 寫「內容從既有 command 剪下」,與 SC-2 before 量測時點的前提「1d 結束時所有原始檔仍未改動」直接矛盾","why":"spec 自己在 1b 用「來源是 plugin skill 無法剪下、只能摘寫」來區分,可見「剪下」= 從來源移除。若照字面在步驟 1 就剪 command,則 1d 結束量 feat-L-before 時 command 已被掏空,before 分母被壓低 —— 這正是反作弊規則 2 要防的「縮分母」,卻由遷移步驟自己觸發;若實際上步驟 1 只複製、步驟 2 才刪,那「剪下」是錯字,兩種讀法會導向不同的量測時序","fix":"步驟 1 改寫為「複製(不刪來源)」並明文「來源側的刪除一律發生在步驟 2」;或把 SC-2 before 量測提前到步驟 1 之前並修正 1d 的敘述"},
{"severity":"P2","location":"design.md:§1 反作弊規則第 2 條","claim":"「X 與 X-before 清單差異只能來自本次實際搬動的檔」照字面排除了 SDD 關閉這個最大單項 —— 它是移除不是搬動","why":"SDD 的 SKILL.md 在 before 清單(Phase 3 會 invoke)、在 after 清單消失,靠的是 skillOverrides 關閉而非搬檔;摘寫產生的 refs/feat-phase3.md 也是新寫不是搬動。一個照規則字面執行的驗證者必須把這兩筆差異判為違規,而它們正是 §4.1 說佔淨刪量絕大部分的槓桿 —— 規則字面與設計主幹互斥,只能靠心證放行,反作弊條款自己開了解釋空間","fix":"改寫為白名單式:「清單差異只允許三類 —— (a) 本次搬動的檔、(b) §4.1 明文關閉的 SDD、(c) §4 檔案樹列出的新建 refs;其餘任何增刪即違規」"},
{"severity":"P2","location":"design.md:§4.2 欄位語意 / §7 步驟 5","claim":"PHASE_REFS 由 manifest 的 phase 欄產生,但沒有規定用哪個 profile、要不要過濾 scope 與 condition","why":"manifest 範例中 phase 欄同時掛在 scope:subagent 列(agents/design-reviewer.md, phase 1)與 condition 列(feat-phase4-fix, phase 4)。天真地「取 phase 欄產 dict」會讓 hook 在 Phase 1 叫主 agent 去 Read reviewer agent 定義檔(打破 SC-2/SC-2b 的帳目分離)、在 Phase 4 無條件注入條件式 ref;且只有 feat-L 一個 after profile,S/M 級 /feat 會被注入 L 級的 ref 集合。三個過濾規則全未寫,步驟 5 紅先行的測試也就沒有判準","fix":"§4.2 明文:PHASE_REFS 只取 scope=main 且無 condition 的列;condition 列由 hook 以「條件句原文」形式注入而非路徑(或乾脆不注入、靠核心指標句);S/M/L 差異本輪接受用 feat-L 並記入 Known Risk"},
{"severity":"P2","location":"design.md:§4.2 <superpowers> 解析 / §7 步驟 0a·11","claim":"before(1d)與 after(步驟 11)兩次量測之間 superpowers plugin 若自動更新,腳本依規定 exit 非 0,而 spec 沒有給任何恢復路徑","why":"「兩次量測解到不同版本即 exit 非 0」是對的防線,但遷移橫跨至少 11 個步驟、可能多個 session,plugin 自動更新是現實會發生的事。觸發後 before 無法重量 —— command 原始檔已在步驟 2 改掉,SC-2 從此永遠算不出來,唯一出路是從 neigui/docs/harness 鏡像還原舊檔重建 before,而這條路沒寫,實作者臨場只能自行發明量法(正是 §1 明文禁止的事)","fix":"1d 量 before 時把逐檔 bytes 連同解到的 plugin 版本一起固化進 baseline.md;版本漂移時規定 after 改用固化的逐檔 bytes 當分母並在報告註明,不 exit 死路"},
{"severity":"P2","location":"design.md:§1 SC-5 量法","claim":"SC-5 的量法指令本身在本環境跑不起來:混用了 bash 大括號展開、grep -rn 與 ripgrep 的 --glob 語法","why":"§0 的立場是「數字一律由量法指令產生」,量法指令就該可執行。`Grep -rn -- \"/code-review\" ~/.claude/{commands,skills,agents} --glob '*.md'` 在 PowerShell(本機主 shell)沒有大括號展開,GNU grep 不認 --glob,Grep tool 不認 -rn —— 三種解讀都不成立;SC-1 同表用的是可貼上執行的 PowerShell,SC-5 卻是偽代碼,步驟 11 執行時得臨場改寫,而改寫出來的範圍(遞迴與否、是否含 refs)沒有 gate 保證與 spec 一致","fix":"改成單一可執行指令,例如 `rg -n --glob '*.md' -- '/code-review' ~/.claude/commands ~/.claude/skills ~/.claude/agents ~/.claude/harness/refs`,並註明必須用 rg"}
]}
```