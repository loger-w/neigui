```json
{"findings":[
  {
    "severity":"P0",
    "location":"design.md:§1 SC-2 門檻 + §4.2「求和不去重」+ §2.1 計帳邊界",
    "claim":"SC-2 的計帳模型在數學上使「搬 ref」只能讓數字變大,after 實測比 before 多 23%,不是降 30%;v11 算出的「可淨刪約 34%」與它自己指定的腳本輸出相反。",
    "why":"§2.1 規定「搬進 refs 的內容全額計入 main scope」,§4.2 又規定「一支 ref 對多個 phase 就列多筆、求和不去重」。before 側的巨石 command 檔只計一次(feat.md 11,194,phase=None),after 側卻是 feat.md 原樣一次 + 十支 ref,其中 review-protocol.md 被 phase 1/2/4 各計一次(4,019×3)、feat-phase0-2.md 計兩次。於是拆檔在此量法下**至少 byte-neutral、只要跨 phase 讀就是 byte-additive**,唯一能讓 SC-2 下降的槓桿只剩「整段刪掉」與關 SDD。實測(`python ~/.claude/hooks/harness_load_estimate.py --profile feat-L{,-before} --scope main`):before 105,001 → after 129,332,即 **+23.2%**;即使 SC-8 通過、把 SDD 的 28,077 從 after 側扣掉,也只有 101,255 = **−3.6%**,連 §4.1 備案的 5% 都過不了。這不是「門檻沒調準」而是**度量方向與改版目標相反**:§1 明說改善在峰值,卻拿一個結構上懲罰拆檔的總量指標當 gate。更糟的是 §1 的處置分支(user 三選一)預設降幅是「正但不足」,對「降幅為負」沒有分支,而反作弊規則第 2 條又禁止調量法 —— 實作者被封死在死路上,而 12 輪來每一輪都在調這個門檻的數字,沒有一輪去跑那支腳本驗證符號。",
    "fix":"把 SC-2 改成**峰值**指標:`max over phase of (command 核心 + 該 phase 的 main-scope 檔)`,腳本加 `--peak`;總量降為與 SC-2b 同級的「有數字、無門檻」欄位。§1 那句「不另立 SC 因為峰值需要定義每 phase 載入集合」已經站不住 —— manifest 的 `phase` 欄就是那個集合,§3 P1 的 `PHASE_REFS` 已經在用它。同時修掉 v11/v12 changelog 裡那組被實測推翻的算式,別讓下一輪繼續在錯的分母上調門檻。"
  },
  {
    "severity":"P1",
    "location":"design.md:§1 SC-2 量法 + §2.1 條件式 + §4.1「不得從帳上憑空消失」",
    "claim":"SC-2 量法沒寫 `--worst`,依 §2.1「否則不計」→ 所有 `condition` 項被排除,直接違反 §4.1 對 `finishing-a-development-branch` 的明文要求,並讓 after 側新變成條件式的 ref 被當成「已刪」。",
    "why":"§4.1 寫「它會在 Phase 8 載入 → 必須列入 manifest 的 condition 項,**不得從帳上憑空消失**」,但 SC-2 指定的指令是 `--profile feat-L --scope main`,沒有 `--worst`,而 §2.1 定義 condition 項「`--worst` 時計入,否則不計」—— 這 7,022 bytes 在兩側都被靜默扣掉,§4.1 的要求由 SC-2 自己的量法作廢。方向性更嚴重的是不對稱:`refs/feat-phase4-fix.md`、`branch-lifecycle/references/exceptions.md`、`refs/auto-wave.md` 改版前是 command / SKILL 內的**無條件 inline 文字**(全額計入 before),改版後變成 condition 項(不計入 after),等於「搬家被記成刪除」—— 正是反作弊規則第 2 條要防的效果,只是發生在 after 側而非 before 側,規則文字沒涵蓋。此外全 spec 沒有任何一句要求 before / after 兩次量測必須用同一組 flag,而 §4.2 只對「解到不同 superpowers 版本」設了 exit 非 0 的保護。",
    "fix":"SC-2 / SC-2b 量法明寫 `--worst`,並在 §1 加一句「before / after 必須以完全相同的 flag 組合量測」;`harness_load_estimate.py` 的 `--before/--after` 對照模式在兩側 flag 或 profile 基準不一致時 exit 非 0(比照既有的版本不一致保護)。反作弊規則第 2 條補一句:profile 之間「condition 欄的增減」也算縮分母,改版前無條件的內容不得在 after 側改標為條件式來換取降幅。"
  },
  {
    "severity":"P1",
    "location":"design.md:§5 SC-3 填寫規則第 3 條 + §3 P1 已知限制 + §5.1/§5.6/§5.8 的條件式 ref 列",
    "claim":"條件式 ref 的「觸發指標句留在來源檔」既沒有被 §5 要求、也驗不到、hook 也涵蓋不了 —— 三道防線同時漏,而這正是本改版的核心風險。",
    "why":"§3 P1 的鐵律是「識別觸發的規則不能延後載入」,§5.1 只對 Phase -1 那列明寫「指標句留核心」,對其餘條件式列(`refs/feat-phase4-fix.md`「收到 test-gap finding」、§5.8 `references/exceptions.md`「撞到異常」、§5.6 `refs/auto-wave.md`「goal_efficiency_mode」)都沒有對應的「指標句留來源檔」列。驗證側同樣空:v10 補的第 3 條規則只強制 `核心`/`保留`/`留原檔` 列填 `present`,`ref` 列的 `present` 依 §5 範例指向**落點檔**,所以實作者把來源檔那句指路句一併刪掉時,SC-3 全綠。機械側也接不住:`refs_for_phase()` 依定義無法判斷條件是否成立,必然要跳過 condition 項(現行實作正是 `if rec.get(\"condition\"): continue`),而 §3 P1 的「已知限制」只列了「Phase -1」與「非 feat 流程」兩項,沒有列「條件式 ref 完全不在 hook 涵蓋範圍」。結果是這類 ref 的載入**完全靠一句沒有任何 gate 的自然語言**,恰好落在 §3 P2 點名最壞的第三種狀態。v10 修 `核心` 列漏覆蓋時是「換模型時沒承接舊極性的覆蓋」,同一個錯型在 `ref`+`condition` 這格再度復發。",
    "fix":"§5 填寫規則第 3 條擴充:凡 `condition` 欄非空的 ref 列,除了指向落點檔的 `present`,**必須再加一個 present 檢查指向來源檔的觸發指標句**(檢查字串就是那句指路句的可辨識片段)。§3 P1 的已知限制補第三項「條件式 ref 不受 hook 涵蓋,全靠來源檔指標句」,並在 §8 風險表「ref 該載入時沒載入」那列一併記入。"
  }
]}
```