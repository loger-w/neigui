```json
{"findings":[
{"severity":"P1","location":"design.md:§1 反作弊規則 2 / §4.1","claim":"反作弊規則 2 的字面(profile 清單差異「只能來自本次實際搬動的檔」)把 SDD 這個最大淨刪項一併禁掉 —— SDD 是被 skillOverrides「關閉」而非「搬動」","why":"SC-2 的 30% 幾乎全靠 SDD 從 after 清單消失(§4.1 自己說「SDD 佔了淨刪量的絕大部分」),但 SDD 的 SKILL.md 沒有被搬動,只是被關閉。嚴格照規則 2 執行,SDD ∈ before∖after 這個差異不合法 → after 必須保留 SDD → 30% 不可達,直接觸發停下回報分支;鬆讀則規則 2 失去判別力 —— 稽核者無法用「是否搬動」區分「關閉 SDD(合法)」與「縮分母(違規)」。這是 v11 新增規則時沒對照 §4.1 機制的傳播缺口,與 v10 抓到的「換模型沒傳播到 §7/§8」同型","fix":"規則 2 改寫為「清單差異只能來自:本次實際搬動的檔,以及 §4.1 明列的 skillOverrides 關閉項(逐檔具名);其餘一律不得增刪」"},
{"severity":"P1","location":"design.md:§7 步驟 1 vs 步驟 0a/1d","claim":"步驟 1「寫 refs(內容從既有 command 剪下)」與 1d 末的前提「此時所有原始檔仍未改動」直接矛盾","why":"「剪下」照字面會在步驟 1 就改動 command 檔(來源側刪除),則 (a) 1d 末量 feat-L-before 讀到的是已瘦身的來源檔,SC-2 分母縮水、降幅失真 —— 這正是反作弊規則要防的基準污染,只是從另一條路徑發生;(b) §5 填寫規則 1 要求 absent 字串「填寫當下在來源檔 grep 命中且唯一」,若來源已被剪走則 1d 根本填不出來。實際意圖顯然是「步驟 1 複製、步驟 2 才刪」,但 spec 沒寫死,而這份 spec 的整個方法論就是不留這種靠善意解讀的縫","fix":"步驟 1 改寫為「複製不剪下;來源側的任何刪改一律發生在步驟 2 之後」,並在 1d 的 before 量測前加一句驗證(如 git-diff 或 hash 比對六個 command 檔與兩支 skill 未變)"},
{"severity":"P1","location":"design.md:§1 SC-6 / §8 風險表","claim":"SC-6 限定「真實 /mod 或 /bug 小案子」,但 §8 兩列風險(SDD 紀律流失、ref 該載入時沒載入)的緩解都指向 SC-6 —— 而這兩個風險按定義只在 /feat 才會發生,SC-6 永遠跑不到","why":"SDD 替代三紀律只在 /feat Phase 3 的 implementer dispatch 情境行使;PHASE_REFS 機械注入依 §3 P1 自承「只涵蓋 /feat」(其餘流程無 state.json)。用 /mod 或 /bug 跑 SC-6,這兩個本次改版最高風險的機制零覆蓋,風險表的緩解欄形同虛設 —— 與 v2「拿 A 配置實測推論 B 配置」是同型錯誤:拿 /mod 的通過推論 /feat 的紀律仍在","fix":"SC-6 改為必須含一次 /feat(允許 S 級最小案),或至少明文承認 /feat 側機制未經真實流程驗證並降級為 Known Risk 記入 feat-improvements.md,不得掛在 SC-6 名下"},
{"severity":"P1","location":"design.md:§5 填寫規則 / §3 P1 / §8","claim":"條件式 ref 的「觸發指標句必須留在來源檔」沒有任何 gate:§5 三條填寫規則都不要求 condition ref 附一筆指向來源檔指標句的 present 檢查,而機械注入又刻意跳過 condition 項(此排除 spec 全文未載明)","why":"條件式 ref 的可發現性完全依賴來源檔的指標句 —— hook 不提醒(實作的 _phase_refs 明確 skip condition 項,但 §3/§4.2/§8 都沒寫這個排除,§8「ref 該載入時沒載入」列還把機械注入當緩解)。無規則強制的結果已可觀察:dispositions.json 裡 auto-wave 與 exceptions.md 兩列各有一筆指標句 present 檢查,feat-phase4-fix 列卻沒有(兩筆 present 都指向 ref 檔本身)。日後任何人「順手清理」feat.md 刪掉 test-gap 指標句,SC-3 全綠、hook 不補位,該 ref 成為永不載入的孤兒 —— 正是 v10 抓的「核心列必填 present」同型漏洞在條件式列上的復發","fix":"§5 加第四條填寫規則:每個帶 condition 的 ref 列必須附一筆 present 檢查,驗「來源檔中的觸發指標句」;§3 P1 與 §8 同步載明機械注入不涵蓋 condition 項"},
{"severity":"P2","location":"design.md:§6.2 升 block 觀察指標","claim":"「連續 10 個非 /feat 流程收尾 warning 誤報 = 0,誤報一次即重置」沒有任何記錄機制 —— 誰數、記在哪、跨 session 怎麼延續都未定義","why":"這是跨多 session 的計數器,靠 agent 自律記憶,正是本 spec P2 原則點名的第三種狀態(寫著當條件但無人驗):實務上不是永遠停在 warning,就是某輪憑印象宣稱達標升 block","fix":"指定機械落點:checker warning 時往固定檔(如 hooks/emoji-warning-ledger.json)append 一筆含分支名與誤報判定,升 block 條件改為「該檔最近 10 筆 false_positive=0」"},
{"severity":"P2","location":"design.md:§1 SC-1 量法","claim":"SC-1 的分母是「commands/ 目錄下 chore.md 以外的所有 .md」而非具名六檔,before/after 之間目錄內容無鎖","why":"SC-2 有「清單差異只能來自搬動檔」的封條,SC-1 沒有等價物。user 已知會多 session 並行動 harness(memory 明載),量測窗口內任何 session 新增一個 command 檔,after 分母被污染而量法無法察覺 —— 現況恰好七檔所以無害,但這是兩個降幅制 SC 中唯一沒鎖清單的量法","fix":"量法改為具名六檔求和(feat/mod/perf/refactor/bug/auto),或在步驟 11 先 assert 目錄檔名集合與 0a 記錄一致"}
]}
```