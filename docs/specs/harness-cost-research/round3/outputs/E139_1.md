```json
{"findings":[
{"severity":"P0","location":"design.md:§5.4 / §5.5 / §5.3(auto-verify 相關列)","claim":"三節對 refactor.md:19、bug.md:19、perf.md:17 的行號與內容宣稱與實檔全部對不上,其中 §5.4 標「刪」的那行實際是 load-bearing 核心規則。","why":"實檔:refactor.md:19 是「characterization test 標 🟢、跟 refactor 的 🔵 分開 commit」(§5.4 自己在別列判為核心的三類分離規則),refactor.md 的 auto-verify 呼叫在 31/32 行,全檔沒有任何抽樣數重述;bug.md:19 是 Phase 3 紅測試先行,Phase 7 在第 26 行,而該行**已經**寫著「呼叫 auto-verify 真實環境節」,與 §5.5「該行沒有呼叫 auto-verify(全檔只有 Phase 6 有)」直接相反;perf.md:17 已經是「Baseline 量測前呼叫 auto-verify 自動化節」,正好等於 §5.3 要求改寫成的目標文字,是 no-op。連帶 §5.7 自稱「抽樣數的唯一 source of truth(§5.4/§5.5 正要刪掉重述)」的前提也不存在。這是 §2.3 自己點名的「描述檔案卻沒讀檔案」錯型復發,而且 12 輪審視中「防漏網」lens 從 v8 起連續回報收斂 —— 它驗的是決議有沒有落點,沒有回頭核對落點的行號指向什麼。後果具體:步驟 1d 依此列填 dispositions.json 時,`absent` 字串若照「行號」取,會把 refactor.md 的三類分離核心規則列為該刪,步驟 2 刪掉後 SC-3 全綠。","fix":"§5.3/§5.4/§5.5 三節重新逐行 grep 實檔後改寫:refactor.md 刪掉「第 19 行抽樣數重述」整列(無物可刪,且行號指向核心規則);bug.md Phase 7 列改為只保留「刪抽樣數重述」以外的部分或整列移除,並修正「沒有呼叫 auto-verify」的錯誤宣稱;perf.md:17 列標記為 no-op 或刪除。同時把 §0 自我約束擴充一條:**本 spec 不得寫入行號,一律寫可 grep 的唯一字串**——行號與衍生數字同樣會漂移,而它比數字更危險(數字錯只是不準,行號錯會刪錯東西)。"},

{"severity":"P0","location":"design.md:§1 SC-2 量法 vs §2.1 條件式定義","claim":"SC-2 的量法沒帶 `--worst`,於是所有標 `condition` 的 ref 都不計入 after,「把內容搬進 ref 並加一個 condition 欄」本身就能製造降幅,而 §1 反作弊規則沒有涵蓋這條路。","why":"§2.1 定義條件式檔由 `--worst` 決定是否計入;SC-2 寫的是 `--profile feat-L --scope main`,不帶 `--worst`。§4 樹裡 `feat-phase4-fix.md`、`auto-wave.md`、`branch-lifecycle/references/exceptions.md` 都明標條件式,而這些內容在改版前是**無條件**躺在 command 檔裡、全額計入 before。這與 §1「搬進 `refs/` 的內容依 §2.1 仍全額計入 main scope(換位置不是刪掉)」正面矛盾——後者是 30% 門檻可達性算式的前提。反作弊兩條只禁「砍核心」與「縮 before 清單」,不禁「在 after 側多標幾個 condition」,而後者是三條路裡最省力、最不像作弊、也最難事後辨識的一條。更直接的問題是量法二義:帶不帶 `--worst` 會得到差距很大的兩個降幅,驗收時無從判定哪個算數。","fix":"SC-2 量法明寫 `--worst`(與「全額計入」的計帳前提一致),或改為兩個數字都報、以 `--worst` 那個對門檻。並在 §1 反作弊規則補第三條:`X` 與 `X-before` 兩側的 `condition` 標記差異必須逐筆列出理由,不得用新增 condition 欄產生降幅。"},

{"severity":"P1","location":"design.md:§7 步驟 5 + §4.2 `phase` 欄語意","claim":"`PHASE_REFS` 由 manifest 的 `phase` 欄產生但沒說要濾掉 `scope: subagent`,會把 reviewer agent 檔的路徑注入主 agent 的每回合提示。","why":"§4.2 範例裡 `agents/design-reviewer.md` 同時有 `scope: subagent` 與 `phase: 1`。步驟 5 只寫「`PHASE_REFS` 由 manifest 的 `phase` 欄產生,不手抄」。照做的話 Phase 1 的注入行會叫主 agent 去 Read reviewer agent 定義——那份檔按 §2.1 定義**不屬於**主 agent 窗口,SC-2 的求和也因為 `scope=subagent` 不會把它算進去。結果是主窗口實際膨脹但量測看不到,SC-2 的數字與真實載入脫鉤,而這正是這次改版唯一的量化成果宣稱。","fix":"步驟 5 明寫「`refs_for_phase()` 只取 `scope == \"main\"` 的項」,並在 `harness_load_estimate.py` 的 pytest 補一個 case:manifest 含 subagent+phase 的項時,產生的 `PHASE_REFS` 不得包含它。"},

{"severity":"P1","location":"design.md:§4.2 腳本模式 + §7 步驟 11","claim":"`--before/--after 對照` 模式在步驟 11 跑會靜默吐出錯誤分母:`feat-L-before` profile 指向的 `commands/*.md` 在步驟 2 之後內容已被改寫。","why":"`-before` profile 的 `path` 幾乎都是同一批 command 檔與 SDD SKILL.md,這些檔在 after 時仍然存在、仍可讀,只是內容已瘦身。所以步驟 11 跑 `--before` 不會報錯、不會 exit 非 0,只會回一個接近 after 的分母,降幅算出來趨近 0 —— 一個完美的靜默失敗。§7 的 1d 註記雖然要求把 before 量測落檔 `baseline.md`,但 §4.2 把 `--before/--after 對照` 列為腳本的正式模式,直接誘導實作者在驗收時一次跑對照;整份 spec 沒有任何一句禁止步驟 11 重跑 `--before`。","fix":"拿掉 §4.2 的 `--before/--after 對照` 模式(它在這個時序下不可能正確),改為腳本只吃單一 profile;步驟 11 明寫「SC-2 的 before 值**只能**取自 `baseline.md`,不得重跑 `--before`」。若要保留對照模式,則腳本必須把 1d 當下的 bytes 值寫進 `baseline.md` 並在 after 對照時比對檔案 hash,不符即 exit 非 0。"},

{"severity":"P1","location":"design.md:§6.4 + §5.5「repro.md 落檔義務」列","claim":"§6.4 把 `.claude/bug/<slug>/repro.md` 當作本輪四項拍板決議之一「新增」,但 bug.md 現行已有四處 repro.md(15 / 18 / 31 行落檔義務 + 42 行 Done 條件「三段齊全」)。","why":"§6.4 描述的三段內容(重現步驟 / root cause 實驗記錄 / Phase 8 反向驗證輸出)正好對應現行 15 / 18 / 31 行,連 Done 條件都已存在。這條決議是 no-op,卻在 §5.5 被標為「核心(新增)」——步驟 1d 會為它填一個 `present` 檢查,而該檢查在改版前就已命中,對「步驟 2 順手刪掉核心規則」這個 §5 規則 3 所要防的風險提供零覆蓋(它從頭到尾都綠)。同時四項拍板決議實際只有三項有內容,§8 風險表與步驟清單卻按四項編排。","fix":"§6.4 改寫為「確認現況已具備,本輪不新增」或整條移除;§5.5 該列的處置由「核心(新增)」改為「核心」,`present` 檢查照核心列規則填來源檔既有唯一字串(這樣它至少能驗到「步驟 2 沒把它刪掉」)。"},

{"severity":"P2","location":"design.md:§7 步驟 1 vs 步驟 1d 註記","claim":"步驟 1 寫「寫 refs(內容從既有 command **剪下**)」,與 1d 註記「此時所有原始檔仍未改動,量到的就是改版前狀態」互斥。","why":"若照字面剪下,步驟 1 就已經動了 command 檔,而 SC-2 的 before 量測排在 1d 結束、步驟 2 之前——量到的是已經被搬空一部分的 command 檔,分母縮水,降幅被系統性低估。這不是作弊方向(對達標不利),但會讓 SC-1 / SC-2 兩個數字建立在不同時間點的基準上(SC-1 的 before 在 0a、改動前;SC-2 的 before 在 1d),而 §1 花了整節論證「分子分母必須同基準」。","fix":"步驟 1 的「剪下」改為「抄寫(來源檔此步不動,刪除一律在步驟 2)」,並在 1d 註記加一句「執行前先確認 `git status` 或 command 檔 mtime 未變」。"}
]}
```