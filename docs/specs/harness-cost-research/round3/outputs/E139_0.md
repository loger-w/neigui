```json
{"findings":[
{"severity":"P0","location":"design.md:§1 SC-4 / §7 步驟 8 / §7 步驟 9 / §8 風險表","claim":"步驟 8 要退役的 `hooks/harness-push-gate.py` 與 `hooks/tests/test_harness_push_gate.py` 早在 2026-07-18(commit 0081c01,design 日期前一週)就已從 harness 與鏡像刪除,兩檔現已不存在。","why":"整條依賴鏈同時失效:(a) SC-4 的 before 量法 `pytest tests/test_harness_push_gate.py --collect-only -q` 對不存在的檔會 exit 4 而拿不到 `G`,步驟 0a 在第一個指令就卡住,而 SC-4 的門檻 `passed ≥ B − G` 沒有 `G` 就無判定基準;(b) 步驟 8 變成 no-op,其「兩檔已在 EXCLUDED,鏡像不受影響」是對現況的錯誤宣稱;(c) v6 唯一的 finding —— 把「更新 SPEC.md/README.md」排到退役之後 —— 整個理由(「步驟 8 會讓 hook 檔數與 pytest 總數變動」)建立在不會發生的變動上,連帶 §7 步驟 9 的驗收條件「檔數與 pytest 數須與步驟 8 之後的實測輸出一致」失去意義。12 輪都在 spec 內部自洽性上推演,沒有人對這兩個檔名做過一次 `ls`,正是 §2.3 點名的「描述既有物件卻沒去看它」錯型。","fix":"刪除步驟 8;SC-4 改為單純 `passed ≥ B 且 failed = 0`(不再有 `G` 這個修正項,新增測試只會讓 passed 上升);步驟 9 的排序理由改寫(檔數變動的真正來源是步驟 1d 新增 `harness_load_estimate.py` 與其測試、步驟 6 新增 emoji 測試,所以步驟 9 仍須排在最後,但理由要換);§8 風險表移除對應列。"},

{"severity":"P1","location":"design.md:§7 步驟 1 vs 步驟 1d","claim":"步驟 1 明寫 refs 內容「從既有 command **剪下**」,步驟 1d 卻宣稱量 `feat-L-before` 時「所有原始檔仍未改動」——兩者不可能同時成立。","why":"若步驟 1 真的是剪下,command 檔在 1d 之前就已被掏空,SC-2 的分母是被污染過的中間態,量到的降幅不是「改版前 → 改版後」而是「已搬走一半 → 改版後」,系統性低估且與 §1 反作弊規則 2(before 側只能反映改版前實際載入)直接衝突;若步驟 1 其實是複製,那步驟 2 才是刪除點,spec 就不該用「剪下」。更麻煩的是這個矛盾會靜默通過所有驗收:SC-3 只驗最終狀態,沒有任何檢查會發現 before 是在錯的時點量的。","fix":"步驟 1 改為「**複製**內容到 refs,來源 command 檔本步驟一律不動」,並在 1d 加一句硬性前置檢查:量 `-before` 前先確認六個 command 檔與 0a 記錄的 bytes 完全相同,不同即停下。刪除來源內容的動作全部收斂到步驟 2。"},

{"severity":"P1","location":"design.md:§7 步驟 5 / §4.2 `phase` 欄語意","claim":"`PHASE_REFS: dict[float, list[str]]` 規定「由 manifest 的 `phase` 欄產生,不手抄」,但 manifest 有 10 個 profile(含 5 個 `-before`)共用同一組 phase 編號,spec 從未定義要取哪些 profile。","why":"實測現行 manifest:`feat-L` 有 phase −1~8、`mod-M` 有 0/2/3/5/6/7/8、`bug` 有 0/2/6/7/9、`refactor`、`perf` 亦然,且每個都有 `-before` 對應。以 phase 為唯一 key 求聯集,/feat Phase 3 會被注入 mod-M 與 refactor 的 ref,而 `-before` profile 列的是**改版前**的載入清單(含被刪或被搬走的舊檔),會把已不存在的路徑注入提示。這正好打穿 §3 P1 想解決的問題本身:注入機制號稱只涵蓋 /feat(其餘流程無 state.json),但資料源卻混進了那些流程的列。","fix":"步驟 5 明寫「`PHASE_REFS` 只取 `feat-L` profile、且排除所有 `-before` profile」,並在 `harness_load_estimate.py` 的 pytest 加一個 case:manifest 裡放一筆 `mod-M` phase 3 的 ref,`refs_for_phase(3)` 不得包含它。長期若要涵蓋其他流程,key 應改為 `(flow, phase)` 而非 `float`。"},

{"severity":"P1","location":"design.md:§5 開頭 vs §1 反作弊規則 1","claim":"§5 只列「需要決策的項目」,未列出者「一律照原樣留在核心」—— 這批未列出的內容既不在反作弊規則 1 的文字涵蓋範圍(它禁的是「§5 **判為**核心的規則」),也完全沒有 SC-3 檢查。","why":"§5 的填寫規則 3 只要求「標 `核心`/`保留`/`留原檔` 的**列**必填 `present`」,而未列出的內容根本沒有列,所以連一個 `present` 都不會產生。SC-1 只留約 1,400 bytes 餘裕、SC-2 只留約 4%,實作者要補這個缺口時,最省力且**字面上不違反任何禁令**的路徑就是砍掉 §5 沒點名的那些段落 —— 它們是整份 command 檔的大宗,砍掉全綠。v12 花了整輪替 SC-1 補處置分支來堵「三面封死只剩砍核心」的出路,但這扇門一直是開的:它不需要砍「判為核心」的規則。","fix":"二選一:(a) 把反作弊規則 1 改為「不得刪除 §5 未標 `刪`/`ref`/`移入` 的任何內容(含未列出者)」,並在步驟 11 加一項驗收:對六個 command 檔逐檔 diff,列出所有被刪但在 `dispositions.json` 中無對應 `absent` 檢查的行,數量須為 0;或 (b) 承認 §5 非窮舉,改用上述 diff 驗收取代「未列出即核心」的口頭保證。"},

{"severity":"P1","location":"design.md:§5.9 / §7 步驟 4","claim":"reviewer 的輸出鐵則與 finding 欄位 schema 從 agent 定義檔移入 `refs/reviewer-preamble.md`,改由 agent「首行 Read」取得 —— 把一條**保證載入**的規則降級成**靠自律**的規則,且無任何 gate。","why":"agent 定義檔的內容是 dispatch 時無條件進 subagent context 的;改成「首行 Read」後,是否載入取決於 subagent 自己照不照做,而 fresh-context subagent 漏一步指示是本 spec 別處反覆承認的失效模式。這直接違反本 spec 自己的 P2(「能被 hook/script 機驗的不寫進 prompt 要求自律;第三種狀態最壞」),而 schema 掉了的後果是 reviewer 回傳非結構化輸出、上游 review loop 解析失敗。§5.9 又自承這次搬動「不省主 agent 窗口」(agent 檔屬 SC-2b),等於用一條可靠性風險換取零 SC-2 收益,收益只有去重防 drift。","fix":"要嘛不搬(去重改以「四檔內容由步驟 7 的鏡像同步器加一條一致性檢查」達成),要嘛保留搬動但補 gate:在 §1 加一條 SC —— SC-6 那輪真實流程中,至少 dispatch 一個 reviewer 並確認回傳含 schema 要求的全部欄位(含雙欄 location);未通過即回退到 agent 檔內嵌。"},

{"severity":"P1","location":"design.md:§1 SC-6 / §8 風險表","claim":"SC-6 允許用「一個真實 `/mod` 或 `/bug` 小案子」收尾,但 §8 把 SC-6 列為「關掉 SDD 後流程紀律流失」與「Workflow 驅動 review 未經真實流程驗證」兩項風險的處置 —— 而這兩項風險都只發生在 /feat。","why":"SDD 三條紀律的移植落點是 `refs/feat-phase3.md`,只有 /feat Phase 3 會讀;§3 P1 的機械 ref 注入亦明寫只涵蓋 /feat(其餘流程無 state.json)。跑 /mod 或 /bug 對這兩者的覆蓋率是零,卻會產出一份「SC-6 通過」的證據,讓最高風險項看起來已被驗證。這是 §2.3「半覆蓋當全覆蓋」錯型在驗收層的復發。","fix":"SC-6 拆兩條:SC-6a 必須是一個真實 **L 級 /feat**(至少跑到 Phase 4),驗 SDD 三條紀律仍被遵守 + `refs_for_phase` 注入的 ref 確實被讀;SC-6b 可用 /mod 或 /bug 驗非 feat 流程的 emoji checker warning 模式與收尾呼叫點。§8 兩列的處置欄改指 SC-6a。"},

{"severity":"P2","location":"design.md:§4.2 `<superpowers>` 解析規則","claim":"「before / after 兩次量測解到不同版本即 exit 非 0」被寫成腳本行為,但兩次量測是相隔九個步驟的**兩次獨立行程**,spec 沒定義解析結果要持久化到哪裡、由誰比對。","why":"單次執行的腳本無從得知另一次執行解到什麼版本,這條 gate 在現行描述下不可實作;實作者最可能的處置是只實作「印出絕對路徑」——而 §4.2 自己已經明說「印出路徑只是資訊不是 gate,解到舊版反而會被判成照規則正確」。於是 v7 花整輪堵的那個洞會以「gate 沒被實作」的形式回來。","fix":"步驟 1d 的量測輸出(含解到的 superpowers 絕對路徑)一律追加寫入 `baseline.md`;腳本加 `--assert-plugin-path <path>` 參數,步驟 11 量 after 時必須帶上 baseline 記錄的路徑,不符即 exit 非 0。把這條寫進 SC-2 的量法欄。"}
]}
```