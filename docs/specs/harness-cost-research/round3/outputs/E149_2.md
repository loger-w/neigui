```json
{"findings":[
{"severity":"P1","location":"design.md:§4.2「一支 ref 對多個 phase → 同 path 列多筆…求和時不去重」+ §1「門檻為何是 30%」","claim":"多 phase 共用的 ref 在 after 側被重複計數,搬進 refs/ 對 SC-2 是「變貴」而非「持平」,v11 推導 30% 可達的算式因此偏樂觀。","why":"§1 說「搬進 refs/ 的內容依 §2.1 仍全額計入(換位置不是刪掉)」,隱含假設是搬動對總量中性。但 §4.2 明訂不去重:原本 inline 在 feat.md 只算一次的內容,拆成 ref 後每個引用 phase 各算一次。實測現行 harness/load-manifest.json 的 feat-L profile:review-protocol.md 出現於 phase 1/2/4 共 3 筆、feat-phase0-2.md 出現於 phase 0/2 共 2 筆 —— 這幾支正是 §5.1 把 feat.md 最大幾塊 review 迴圈內容搬過去的落點。換位置在計帳上是 ×3,不是 ×1。v11 算出「可淨刪 34%、故 30% 有餘裕」時沒有扣掉這筆倍增,餘裕可能是負的,而一旦不足,§1 三條禁令(不准砍核心/不准縮分母/不准調量法)會把實作者逼進與 SC-1 完全相同的死角。","fix":"在 §1 或 §4.2 明寫多筆列的計帳選擇並重算門檻:或(a)承認倍增、把 SC-2 門檻依含倍增的實算值下修;或(b)改為「同 profile 同 path 只計首次 + 其餘按重讀成本折算」,並把該規則寫進 §2.1 計帳邊界。無論選哪個,都要在步驟 1d 用 --profile feat-L 實跑一次 after 側預估值來檢核門檻可達性,而不是沿用 v11 的手算。"},

{"severity":"P1","location":"design.md:§1 SC-2 量法(無 --worst)+ §2.1 條件式定義 + §5.1 feat-phase4-fix.md / §5.6 auto-wave.md / §5.8 exceptions.md","claim":"把原本無條件 inline 的內容重新分類為 condition 項,就能在 SC-2 的預設量法下 100% 從 after 側消失 —— 這是 §1 兩條反作弊規則沒堵到的第三條作弊路徑。","why":"SC-2 量法是 `--profile feat-L --scope main`,不帶 `--worst`,依 §4.2「condition:--worst 時計入,否則不計」,所有條件式 ref 在 SC-2 中一律歸零。但這些內容在 before 側是 feat.md / auto.md / branch-lifecycle SKILL.md 檔內的一部分,以整檔 bytes 全額計入分母。於是「Phase 4 修 finding 操作」「goal_efficiency_mode 整節」「異常處理表」等大段落,只因為被加上一個 condition 字串,就在降幅上被記成純刪除 —— 與 §1 自己主張的「換位置不是刪掉」直接矛盾,而且比它明文禁止的兩條路徑更好走。實測 manifest 中 feat-L 有 4 筆 COND、feat-L-before 只有 3 筆,差額正是 feat-phase4-fix.md。","fix":"擇一:(a) SC-2 的量法固定加 `--worst`(最壞情況才是「窗口會不會爆」的真問題),before/after 同法;(b) 保留非 --worst 量法,但在 §1 反作弊規則新增第 3 條:「改版前無條件載入的內容,改版後不得僅因搬進條件式 ref 而從 SC-2 分子消失;此類項在 SC-2 中一律按無條件計入」,並要求同時報 --worst 與非 --worst 兩個降幅。"},

{"severity":"P1","location":"design.md:§1 SC-6 + §8 風險表「關掉 SDD 後流程紀律流失」「ref 該載入時沒載入」兩列","claim":"SC-6 明文只跑 /mod 或 /bug,但 §8 用它當作兩條 /feat-only 風險的唯一緩解,該緩解實際上打不到要驗的東西。","why":"SDD 三條紀律的落點是 refs/feat-phase3.md(§5.1 / 步驟 1b),只有 /feat Phase 3 會載入;§3 P1 的 PHASE_REFS 機械注入也自陳「只涵蓋 /feat(其餘流程無 state.json,hook 不觸發)」。跑一輪 /mod 或 /bug 既不會經過 Phase 3、也不會觸發 ref 注入,所以「SC-6 真實跑一輪驗紀律仍在」對「關掉 SDD 後紀律流失」是零覆蓋。而本次改版風險最高的兩件事恰恰是:feat.md 被拆成 8 支 ref 後該載入時載不載得到、以及 SDD 關掉後 Phase 3 紀律還在不在 —— 兩者在整份 spec 的驗收裡沒有任何一條真的碰到。","fix":"把 SC-6 拆成兩條:SC-6a 沿用小型 /mod 或 /bug(驗非 feat 流程的核心檔仍可獨立運作);SC-6b 必須跑一輪真實 /feat(至少到 Phase 4),證據要求明列「Phase 3 的 ledger 檔實際產生」「每 task review gate 有執行」「hook 注入的第四行在對應 phase 出現正確 ref 清單」。§8 兩列的處置欄改指 SC-6b。"},

{"severity":"P1","location":"design.md:§1 反作弊規則第 2 條 vs §4.1 / §1「門檻為何是 30%」","claim":"「X 與 X-before 的清單差異只能來自本次改版實際搬動的檔」照字面會禁止把 SDD 從 after profile 移除,而那正是本次唯一的大槓桿;spec 從未定義「被 skillOverrides 關掉的 skill」如何在 profile 上表達。","why":"SDD 不是被搬動的檔,是被 skillOverrides 關掉的 plugin skill,不符合該條許可的唯一差異來源。同時 §4.1 備案只說「未通過就不關 SDD、接受它仍佔 context」,沒有反向規定「通過後 after profile 應移除該筆」。這個空缺已經實體化:現行 harness/load-manifest.json 的 feat-L(after)仍列著 `<superpowers>/skills/subagent-driven-development/SKILL.md`(main, phase 3),等於把號稱佔淨刪量絕大部分的 28,077 又算回分子,SC-2 的 30% 在這份 manifest 下結構性不可達 —— 而實作者若照 §1 字面辦事,連移除它都會被判成作弊。","fix":"§1 反作弊規則第 2 條的許可清單補上第二種合法差異:「因 SC-8 通過而被 skillOverrides 停用的 skill,自 after profile 移除;停用清單須逐筆具名在 §4.1」。同步在步驟 10 之後加一句「更新 load-manifest.json 的 feat-L,移除已停用 skill 的所有列」,並讓 harness_load_estimate.py 在 after 側偵測到仍列有已停用 skill 時 exit 非 0。"},

{"severity":"P2","location":"design.md:§7 步驟 1(「寫 refs(內容從既有 command 剪下)」)vs 步驟 0a 附註(SC-2 before「此時所有原始檔仍未改動」)","claim":"步驟 1 用「剪下」描述建 refs,而 SC-2 的 before 量測排在 1d 之後,並宣稱此時原始檔未動 —— 兩者只有在步驟 1 其實是「複製」時才成立,spec 沒有寫明。","why":"若實作者照字面「剪下」,建 refs 當下 feat.md / auto.md 已縮水,1d 之後量到的 feat-L-before 就不是改版前狀態,SC-2 的分母被污染(方向上會低估降幅,但更關鍵的是這個量測點的語意變成未定義,而 §1 又禁止事後調量法)。步驟 2「逐檔改寫 command」才是刪除點,兩步的動作邊界目前只能靠推論。","fix":"步驟 1 的括號改成「內容從既有 command **複製**(來源檔此步不刪,刪除一律在步驟 2)」,並在 1d 的 SC-2 before 註記補一句前置檢查:量測前先確認六個 command 檔 bytes 總和 = 步驟 0a 記錄的 before 值,不一致即停下。"}
]}
```