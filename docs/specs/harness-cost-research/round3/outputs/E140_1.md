```json
{"findings":[
{"severity":"P1","location":"design.md:§4.1 備案","claim":"SC-8 失敗的備案(feat.md 寫一行負向指示)靠的正是本 spec 自己 F2 判定「實測失守」的 prompt 自律,而機械替代方案(PreToolUse hook 對 Skill 呼叫 SDD 直接 deny)從未被評估","why":"P2 原則明言「能機驗的不寫進 prompt 要求自律」,備案卻是一條無 gate 的 prompt 禁令 —— 正是 P2 點名「最壞的第三種狀態」。hook deny 可讓 SKILL.md 本體(約佔 before 兩成)永不載入、只剩 description 常駐,等於在 skillOverrides 之外還有一條能保住主線大部分槓桿的路。實測結果(sc8-probe FAIL → 走備案 → SC-2 誠實計入 SDD 後不降反增 +4.4%)證明這個缺口直接決定了整案成敗","fix":"備案改為(或至少增列評估)PreToolUse hook:matcher Skill、對 subagent-driven-development 回 deny;通過反向驗證(嘗試 invoke 必被擋)後,feat-L after profile 可正當移除 SDD 本體、只計 description 行"},
{"severity":"P1","location":"design.md:§4.1 備案 / §8 風險表","claim":"備案的 SC-2 門檻 ≥5% 沒有做過 v11/v12 對 30%/25% 做的那種可達性驗算 —— 在「SDD 誠實留在 after 側 + 跨 phase 讀取不去重」的計帳邊界下,5% 由建構上不可達","why":"搬進 refs 的內容被多 phase 重複計入只會讓 after 總量上升,淨刪只剩 rationale 與重疊條;同一份算式在 v11 時就能算出負值。這是「門檻不可達」錯型的第三次復發(v5 SC-1、v11 SC-2、本處備案),v12 自己總結的教訓「只補其中一個,錯型會在另一個復發」沒有套用到備案門檻上。實測 −4.4%(增加)已確認","fix":"備案門檻同樣以逐項算式驗算後設定;若算式顯示為負,備案應直接改為「SC-2 換指標(phase 峰值降幅)」而非設一個註定 FAIL 的總量門檻"},
{"severity":"P1","location":"design.md:§1 SC-6 / §7 遷移步驟 / §8 風險表","claim":"SC-6 沒有任何遷移步驟負責執行(步驟 0-11 無一排入真實案子),且其範圍(/mod 或 /bug)完全碰不到本次改版風險最高的三個面:PHASE_REFS hook 注入(僅 /feat 有 state.json)、feat-phase3 的 SDD 替代紀律、條件式 ref 的載入時機","why":"§8 風險表宣稱「關掉 SDD 後流程紀律流失 → SC-6 真實跑一輪驗紀律仍在」,但 /mod、/bug 根本不走 Phase 3 implementer 紀律也不觸發 phase 注入 —— 該風險的驗收是空集合。實測結果 SC-6 直接「未執行」而其他 SC 都驗完,正是無人認領的結構後果","fix":"SC-6 拆兩條:(a) 指定步驟 11 前必須排入一個真實小案子,且 (b) 至少一次 /feat S 級流程以驗 PHASE_REFS 注入與 feat-phase3 紀律;§8 該列的處置改引 (b)"},
{"severity":"P1","location":"design.md:§1 反作弊規則第 2 條","claim":"「X 與 X-before 的清單差異只能來自本次實際搬動的檔」照字面禁止了主線自己的最大槓桿 —— SDD 是被 skillOverrides 關閉,不是被搬動,從 feat-L after 清單移除它即違反此規則","why":"SC-8 通過的主線下,實作者要嘛違反反作弊規則把 SDD 移出 after 清單,要嘛遵守規則保留它而讓 30% 必不可達 —— 規則與主線機制互斥。12 輪都在防「縮 before」,沒人檢查這條規則對 after 側的字面效力","fix":"規則補一句:「或來自本次以機械手段(skillOverrides / hook deny)關閉且經對應 SC 驗證的 skill」"},
{"severity":"P2","location":"design.md:§1 SC-2 量法 / §4.2 condition 欄","claim":"SC-2 的量法指令未指定 --worst,condition 項預設不計入 after 總和 —— 給 ref 標 condition 就能把它從 SC-2 分子上消掉,是反作弊規則沒覆蓋的第三條作弊路","why":"§1 宣稱「搬進 refs 的內容仍全額計入 main scope」只對無條件 ref 成立;phase 語意(「Phase 3 才讀」)與 condition 語意(「特定分支才載入」)界線模糊,實作者可辯稱任何 phase-ref 都是條件式。反作弊規則只封了「砍核心」與「縮 before 分母」兩條","fix":"SC-2 量法明寫加 --worst(before/after 同 flag);反作弊規則第 3 條:condition 欄只准用於既有表中已標「條件式」的檔,新增 condition 標記視同調量法"},
{"severity":"P2","location":"design.md:§4.2 -before profile / §7 步驟 11","claim":"-before profile 列的是活的實體檔路徑,步驟 2 改寫 command 後它量到的是改寫後大小 —— 步驟 11 依 §4.2 的 --before/--after 對照模式算降幅必然失真","why":"降幅制的分母在量測當下已不存在於檔案系統。實測已踩中:改版後跑對照模式得到 −23.17% 的錯誤數字,靠實作者臨場改用 baseline.md 的凍結值才救回 —— 這個補救是量法即興,正是 §0 想消滅的東西","fix":"§4.2 增列:1d 量測時把每檔 bytes 凍結寫入 baseline(或 manifest 的 -before 直接帶 size 欄),步驟 11 的分母一律取凍結值,腳本偵測到 -before profile 的 path 現值與凍結值不符時改用凍結值並註記"},
{"severity":"P2","location":"design.md:§7 步驟 1 vs 步驟 0a 註記","claim":"步驟 1 寫「refs 內容從既有 command 剪下」,但 0a 註記與 1d 的正當性都建立在「步驟 2 之前所有原始檔仍未改動」上 —— 「剪下」的自然讀法(來源同步移除)發生在 1d 之前,兩處矛盾","why":"實作者若在步驟 1 真的剪(而非先複製、步驟 2 才刪),1d 量到的 feat-L-before 就是已瘦身的狀態,SC-2 分母被靜默污染,且反作弊規則對這種「無意的縮分母」無從辨識","fix":"步驟 1 改寫為「複製進 refs,來源暫留;來源側的移除一律在步驟 2 執行」"},
{"severity":"P2","location":"design.md:§4.2 phase 欄 / §3 P1","claim":"PHASE_REFS 由 manifest 的 phase 欄產生,但沒有指定取自哪個 profile、也沒說 subagent scope 與 condition 項要不要過濾 —— 三個都會直接改變 hook 注入內容","why":"manifest 有十個 profile;/feat S 級跑起來若吃 feat-L 的 refs 清單會注入用不到的檔;agents/design-reviewer.md 是 subagent scope 卻帶 phase:1,照字面會被當 main agent 的 phase-1 ref 注入;condition 項(feat-phase4-fix)若無條件注入,condition 機制形同虛設,若永不注入則「ref 該載入時沒載入」在條件分支上復發","fix":"§4.2 明寫:PHASE_REFS 取固定 profile(如 feat-L)、過濾 scope != main、condition 項注入時附帶條件文字(「收到 test-gap finding 才讀」)而非直接列檔"}
]}
```