```json
{"findings":[
  {
    "severity":"P1",
    "location":"design.md:§1 反作弊規則第 2 條",
    "claim":"「兩個 profile 的檔案清單差異只能來自本次改版實際搬動的檔」這句字面上把 SC-2 的全部淨刪來源都排除了,使 SC-2 在建構上恆為 0% 降幅。",
    "why":"本次改版的淨刪量只有兩類來源:(a) 關掉 SDD —— 檔案沒被搬動,只是 skillOverrides 讓它不再載入;(b) §5 標「刪」的段落(git add -A、鐵則重述、日期敘事)—— 是刪除或搬進執行期永不載入的 RATIONALE.md,同樣不是「搬動的檔」。而 SC-2 的分母是「檔案清單求和」,不是段落層級的 bytes 差:command 檔改版前後都在兩個 profile 裡,只是變小,清單本身無差異。嚴格照這條規則,after profile 唯一被允許移除的項目一個都不存在 —— §1 自己算給 SC-2 的「約 34% 可達值」有 28,077 來自 SDD,恰恰是這條規則字面禁止的那項。這是 v11 為堵作弊而新增的規則反過來封死達標路徑,屬於「修正自己引入新問題」的第三次復發(v6→v7、v7→v8 各一次)。",
    "fix":"把規則從「檔案清單差異」改寫為白名單式的三類合法差異:[1] 因 skillOverrides 而不再載入的 skill(須具名,且 SC-8 通過)、[2] 因搬進 refs/ 而改變 path 的檔(仍全額計入,不得移除)、[3] 因刪除而消失的檔(整檔刪除才適用)。並明寫「同一支 command 檔在兩側清單皆存在、僅 bytes 變小,是預期且主要的降幅來源」,以免實作者誤讀成「清單一樣就等於沒降」。"
  },
  {
    "severity":"P1",
    "location":"design.md:§7 步驟 5 / §4.2 `phase` 欄語意",
    "claim":"「`PHASE_REFS` 由 manifest 的 `phase` 欄產生,不手抄」沒有 profile 維度,而 manifest 是多 profile 的;且未定義是 build-time 生成還是 runtime 讀取,兩種都各自有未處置的失效模式。",
    "why":"manifest 有 feat-L / mod-M / bug / refactor / perf 五組(各含 -before)profile,每組都有自己的 `phase` 欄,但 `PHASE_REFS: dict[float, list[str]]` 只有 phase 一個 key。mod-M 的 phase 4 與 feat-L 的 phase 4 是完全不同的流程階段,合併求值會讓 /feat Phase 4 被注入 mod 的 ref 指標。spec 也沒說只取 feat-L —— 而 §3 P1 明講此機制只涵蓋 /feat,兩處對不上。生成時機同樣未定:若 runtime 讀 JSON,manifest 損壞或路徑解析失敗會拋例外,而 §3 P1 自己指出該例外會被 `harness-context.py` 的兩層 except 吞掉 → 整段注入靜默消失,正是這條原則寫來避免的失效模式;若 build-time 生成成靜態 dict,則「不手抄」只在生成當下成立,之後 manifest 與 `PHASE_REFS` 漂移沒有任何 SC 驗得到(SC-3 只掃 dispositions.json,SC-7 只驗鏡像)。",
    "fix":"步驟 5 明寫三件事:[1] 只讀 `feat-L` profile(或在 manifest 為 profile 加 `flow` 欄,`refs_for_phase(flow, phase)` 帶流程參數);[2] 採 runtime 讀取,並在 `harness_lib.py` 內就地 catch 到「manifest 缺失/壞掉」時回傳空 list 且**印出可見警告**,不讓它冒到會被吞掉的外層;[3] 補一個測試斷言 `refs_for_phase()` 對 manifest 中每個 feat-L phase 的輸出與 manifest 內容一致,把漂移變成機驗。"
  },
  {
    "severity":"P1",
    "location":"design.md:§1 SC-4",
    "claim":"SC-4 只比對 passed 的「數量」,不比對 test nodeid 的「集合」,因此「弄壞或刪掉既有測試、同時新增更多測試」會全綠。",
    "why":"步驟 5、6 都紅先行,會新增可觀的 case 數,使 `passed ≥ B − G` 這個不等式留下大量餘裕。實作者在改 `harness_lib.py`(步驟 5)與 `check_feat_tags.py`(步驟 6)時弄壞既有 case,只要把壞掉的測試刪除或改 assertion,`failed = 0` 仍成立、`passed` 仍遠高於 `B − G` → SC-4 通過。而 SC-4 的標題與意圖是「hook 測試不退步」。這條在 v4 從等式改成不等式時,只解決了「恆 FAIL」,沒注意到不等式把「不退步」弱化成「總數不減」;spec 對 `check_feat_tags.py` 既有 TestMain 的相容性分析(§6.2 第 6 點)也顯示既有 case 確實會被這次改動掃到,不是理論風險。順帶:`G` 用 `--collect-only` 計數、`B` 用 passed 數,若 push-gate 測試含 skip/xfail 兩者單位不同,`B − G` 會偏低,再鬆一層。",
    "fix":"SC-4 改為集合比對:步驟 0a 除了記 `B`,另存 `pytest tests --collect-only -q` 的完整 nodeid 清單到 `baseline.md`;步驟 11 要求「before 的 nodeid 集合,扣掉 `test_harness_push_gate.py` 的全部 nodeid 後,必須是 after nodeid 集合的子集」,外加 `failed = 0`。這同時消掉 `B`/`G` 單位不一致的問題。"
  },
  {
    "severity":"P1",
    "location":"design.md:§8 風險表「關掉 SDD 後流程紀律流失」列 vs §1 SC-6",
    "claim":"風險表把 SC-6 當成「SDD 紀律移植是否有效」的緩解,但 SC-6 允許用 `/mod` 或 `/bug` 小案子,而這兩個流程根本不會走到移植點,緩解與驗收互不相交。",
    "why":"三條移植紀律(每 task review gate、ledger、禁並行 dispatch implementer)寫在 `refs/feat-phase3.md`,只在 `/feat` Phase 3 載入;`/mod` 與 `/bug` 沒有 Phase 3 實作分流、不讀該 ref、也不會觸發 SDD 的 description 匹配條件(「≥3 檔且彼此獨立」)。同理,本次改版最結構性的幾項 —— 七支 feat-phase*.md 的拆分、`PHASE_REFS` 的機械注入(§3 P1 明講只涵蓋 /feat)、Phase -1 指標留核心 —— 在 /mod 或 /bug 一輪裡一項都驗不到。§8 另一列「Workflow 驅動 review 未經真實流程驗證 → SC-6」也有同樣問題的較輕版本。結果是:風險表上兩條最重的風險,其具名緩解措施在允許的執行方式下可能完全沒被觸及,而 SC-6 仍然會被判為通過。",
    "fix":"SC-6 拆成兩條或加下限:必須包含**一次真實 `/feat`(至少 M 級)**,並在證據中明列「Phase 3 是否載入 `refs/feat-phase3.md`、三條紀律是否實際被遵循、hook 第四行是否在各 phase 印出正確 ref」;`/mod` 或 `/bug` 那輪保留為跨流程回歸。若不願付一輪 /feat 的成本,就把 §8 那兩列的緩解欄改成「未驗證,列入 Known Risk」,不要記成已被 SC-6 覆蓋。"
  },
  {
    "severity":"P1",
    "location":"design.md:§9 / §8 風險表末列 vs §7 遷移步驟",
    "claim":"「本輪結束後建議還原 `protect-harness.py`」是一項已拍板的決議,但 §7 十二個步驟裡沒有任何落點,也沒有 SC 驗收 —— 正是本 spec 自己在 v7 / v8 反覆點名的「決議無落點會靜默落空」錯型。",
    "why":"§8 風險表最後一列與 §9 都寫明停用期間「防 prompt injection 靜默弱化強制層」這道防線不存在,並給了可貼回 `settings.json` 的完整 JSON —— 也就是說 spec 認定它該還原、也備好了怎麼還原,唯獨沒說什麼時候由誰做。§7 步驟 11 是最後一步(SC 驗證),之後沒有收尾步驟。這與 v7 抓 §6.3、v8 抓 §6.1 的漏洞完全同型:有決議、有理由、沒有可執行落點。而且這一項的失效是永久性的 —— 沒人會回來看一份已結案 spec 的 §9,結果就是這道防線無限期停用,而本輪新增的 `harness/refs/` 又「本來就不在其保護樣式內」,保護缺口比停用前更大。",
    "fix":"§7 新增步驟 12:還原 `protect-harness.py` 兩處註冊(JSON 見 §9),並**同時把 `harness/refs/**` 與 `harness/*.json` 加進其保護樣式**(否則新落點依舊裸奔);驗收方式是重啟 session 後對 `harness/refs/` 下任一檔發一次 Edit,確認跳出 ask 確認框。若 user 決定長期不還原,就把 §9 從「建議還原」改寫為明確的「已接受此風險,不還原」,並移除 §8 那列的緩解措辭 —— 兩者擇一,不要留在中間態。"
  },
  {
    "severity":"P1",
    "location":"design.md:§5 SC-3 驗證模型 / §7 步驟 1d",
    "claim":"`absent` 檢查是 fail-open 的:檢查的 `file` 欄沒有定義路徑基準,且「檔案讀不到」與「字串不存在」在語意上無法區分,兩者都會被判為通過。",
    "why":"檢查寫成 `{\"kind\":\"absent\",\"file\":\"commands/auto.md\",\"string\":\"…\"}`,是相對路徑,但 spec 從未說相對於什麼。§4.2 為了 `<superpowers>` 解析特地引入 `project_root` 必填欄並花整段論證「絕對不能用 process cwd」(理由:SC-4 的量法會 `cd ~/.claude/hooks`),`dispositions.json` 卻完全沒有對應的 root 宣告 —— 同一支腳本、同一個 cwd 陷阱,只堵了一邊。若腳本從 `~/.claude/hooks` 跑,`commands/auto.md` 解不到,所有 `absent` 全部「不命中」= 全綠。即使路徑基準正確,這個 fail-open 語意本身仍在:實作者若把某個來源檔改名或整檔刪除,該檔上所有 `absent` 檢查一律通過。§5 第 3 條填寫規則只保證「核心/保留列必填 present」,對「只刪不留」的列(§5.1 的 `git add -A`、鐵則重述、日期敘事等多列)完全沒有 present 覆蓋,那些列在上述兩種情況下都是恆綠。SC-3 是三大驗收之一,它的假綠會讓整個「規則有沒有被弄丟」的防線失效 —— 而那正是本次改版的實質風險。",
    "fix":"兩項都要:[1] `dispositions.json` 頂層加必填 `\"root\": \"C:/Users/USER/.claude\"`,所有 `file` 相對於它解析,與 §4.2 的 `project_root` 同一套紀律;[2] 腳本對每個檢查先斷言 `file` **存在且可讀**,不存在即 exit 非 0 並具名報出(不是視為 absent 通過),並在輸出中印出「共檢查 N 個檔、M 個 absent、K 個 present」的計數,讓「掃了 0 個檔卻回報通過」不可能被誤讀成綠燈。步驟 1d 的 pytest 要含一個 case:故意指一個不存在的 `file`,腳本必須 exit 非 0。"
  },
  {
    "severity":"P2",
    "location":"design.md:§1「所有門檻以 bytes 表達」 vs §4.2 `harness_load_estimate.py`",
    "claim":"SC-1 的量法明確是 bytes(PowerShell `Length`),SC-2 的腳本則沒規定用 bytes 還是字元數,而語料是中英混雜,兩種算法會給出實質不同的降幅。",
    "why":"§1 特地寫「所有門檻以 bytes 表達(不用 KB,避免 10^3 / 2^10 歧義)」,顯示 spec 在意單位歧義,卻只約束了門檻的表達、沒約束 estimate 腳本的實作。Python 預設 `open()` 讀成 str,`len()` 是字元數;UTF-8 下中文一字 3 bytes、ASCII 一字 1 byte。本次分子分母的組成極度不均:要刪掉的 SDD SKILL.md 是英文(bytes ≈ chars),要保留的 command 核心與被搬走的中文敘事則是 3:1。同一份改動在 bytes 與 chars 兩種算法下的降幅可以差到十個百分點以上,而 SC-2 的門檻只剩約 4 個百分點的餘裕(§1 說可達 34%、門檻 30%)。實作者選錯單位,就會落進 §1「未達標處置」那條停下回報的分支 —— 或反過來拿一個非預期的寬鬆數字宣告達標。",
    "fix":"§4.2 明寫腳本以 `Path.stat().st_size`(或 `open(..., 'rb')` 後 `len()`)計 bytes,與 SC-1 的 `Measure-Object Length` 同單位;並在腳本輸出的表頭印出 `unit=bytes`。步驟 1d 的 pytest 加一個含中文的 fixture 檔,斷言回報值等於其 UTF-8 位元組數。"
  }
]}
```